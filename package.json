{
  "name": "eigendesk-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/desktop",
    "plugins/*",
    "examples/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:app": "node scripts/build.mjs",
    "dev": "npm run tauri -w @eigendesk/desktop -- dev",
    "build:release": "npm run tauri -w @eigendesk/desktop -- build",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm run -ws --if-present typecheck",
    "test": "vitest run",
    "lint": "eslint .",
    "wb": "node packages/devtools/bin/wb.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "dompurify": "^3.2.3",
    "esbuild": "^0.24.2",
    "eslint": "^9.17.0",
    "marked": "^15.0.4",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vite": "^6.0.5",
    "vitest": "^2.1.8",
    "zustand": "^5.0.2"
  }
}
