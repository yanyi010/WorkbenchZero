[package]
name = "workbench-zero"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Workbench Zero — a fast, local-first, extensible personal workstation"
default-run = "workbench-zero"

[lib]
name = "workbench_zero_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
wz-kernel = { workspace = true }
wz-permissions = { workspace = true }
wz-plugin-runtime = { workspace = true }
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-updater = "2"
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
