[workspace]
resolver = "2"
members = [
  "crates/permissions",
  "crates/commands",
  "crates/events",
  "crates/settings",
  "crates/storage",
  "crates/secrets",
  "crates/artifacts",
  "crates/search",
  "crates/workspace",
  "crates/plugin-runtime",
  "crates/kernel",
  "crates/mcp-server",
  "apps/desktop/src-tauri",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["EigenDesk contributors"]

[workspace.dependencies]
eigendesk-permissions = { path = "crates/permissions" }
eigendesk-commands = { path = "crates/commands" }
eigendesk-events = { path = "crates/events" }
eigendesk-settings = { path = "crates/settings" }
eigendesk-storage = { path = "crates/storage" }
eigendesk-secrets = { path = "crates/secrets" }
eigendesk-artifacts = { path = "crates/artifacts" }
eigendesk-search = { path = "crates/search" }
eigendesk-workspace = { path = "crates/workspace" }
eigendesk-plugin-runtime = { path = "crates/plugin-runtime" }
eigendesk-kernel = { path = "crates/kernel" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
anyhow = "1"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled"] }
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream", "json", "blocking"] }
futures-util = "0.3"
portable-pty = "0.8"
notify = "6"
keyring = { version = "3", features = ["sync-secret-service", "crypto-rust"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
tracing-appender = "0.2"
dirs = "5"
sha2 = "0.10"
zip = "2"
walkdir = "2"
once_cell = "1"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
